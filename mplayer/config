# for key bindings, see input.conf
#conf=$HOME/.mplayer/input.conf
fs=1	        #always fullscreen
idx=yes         #seek downloading file (works?)
af=scaletempo	#pitch correction on !=1x playback.
mixer-channel=Master #same as dwm
vo=xv           #fixes libvdpau_nvidia.so error
#vo=x11           #to work under VM
ao=alsa 
dvd-device=/dev/sr0   # use: mplayer dvd:// OR dvdnav:// -nocache --mouse-movement for menus
cdrom-device=/dev/sr0 # use: mplayer cdda://
nojoystick=1

slang=en,eng
msgcolor=yes
msgmodule=yes

# NOTES
# mplayer dvdnav:// -dvd-device ~/dvd.iso (play iso)
# mplayer -ss 10 (start at 10s, skip TED intros)

#really-quiet=1
#ss=1               #always start at Nsec
#endpos=13          #end Nsec after start
#loop=0             #loop to infinity (0)
#softvol=yes        #software volume on
#softvol-max=400    #software volume boost 400%
